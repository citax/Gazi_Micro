#include <main.h>

#include <lcd.c>
#fuses HS,NOWDT,NOPROTECT,NOLVP
#use delay(clock=20000000)

void main() {
lcd_gotoxy(1,3);
   char text[]="Merhaba Dunya!";
   int i;

   lcd_init();
   lcd_gotoxy(1,3);

   for(i=0; i<16; i++) {
      lcd_putc(text[i]);
   }

   lcd_gotoxy(1,4);

   for(i=16; i<32; i++) {
      lcd_putc(text[i]);
   }

   while(TRUE) {
      // Sonsuz döngü
   }
}
